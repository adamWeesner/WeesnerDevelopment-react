export default class FederalIncomeTax {
    constructor(year, maritalStatus, payPeriod, over, notOver, plus, percent, nonTaxable) {
        return { year, maritalStatus, payPeriod, over, notOver, plus, percent, nonTaxable }
    }
}