const styles = theme => ({
    horizontal: {
        display: 'flex',
        minWidth: 450
    },
    formControl: {
        margin: theme.spacing(),
        minWidth: 120,
    },
    typography: {
        marginLeft: theme.spacing(),
        marginTop: theme.spacing() * 2.5,
        fontSize: 16
    },
})

export {
    styles,
}